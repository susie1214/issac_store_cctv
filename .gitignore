# ── 가상환경 ──────────────────────────────────
venv/
.venv/
env/

# ── Python ────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# ── 대용량 모델 파일 (.pt / .onnx / .caffemodel)
# ※ 모델은 git에 올리지 않고 각자 다운로드
*.pt
*.onnx
*.caffemodel
*.pb
*.tflite
models/opencv_face_detector_uint8.pb

# ── 로그 / 보고서 (자동 생성됨) ───────────────
logs/

# ── 설정 (비밀번호 포함) ──────────────────────
config.json

# ── 구역 설정 (환경마다 다름) ─────────────────
zone_config.json

# ── OS / IDE ──────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ── 환경변수 (API 키 등 절대 올리지 말 것) ────
.env

# ── Claude Code ───────────────────────────────
.claude/

# ── 오디오 파일 ───────────────────────────────
*.m4a
*.mp3
*.wav

# ── 기타 ──────────────────────────────────────
*.log
run.log
